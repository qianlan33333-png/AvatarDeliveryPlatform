# 设计壳接口待办

以下两项在设计稿中有完整界面，但当前仓库没有可可靠调用的接口。界面已按设计保留，未伪造数据或成功状态。

1. 课程封面对象存储直传
   - 已有：3:4 尺寸校验、JPG/PNG、2MB 限制、本地预览、手填 HTTPS CDN URL。
   - 缺少：一次性图片上传签名接口、对象存储目标域名与回写回调。
   - 建议契约：`POST /admin/media/images/upload-signature` 返回签名、对象 key、上传地址和公开 CDN URL。

2. 播放过载请求 ID
   - 已有：容量错误码、在线人数、100 人上限和“已有用户不受影响”提示。
   - 缺少：服务端统一 `request_id` 生成与错误响应字段。
   - 建议契约：所有 API 响应头返回 `X-Request-ID`，错误体同步返回 `request_id`。
