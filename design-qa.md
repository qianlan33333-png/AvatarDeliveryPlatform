# 课程列表与详情页 Design QA

## 对比基准

- 视觉源（课程列表）：`/var/folders/dq/56xlfzsx05zc7vqhbl7lgv6c0000gn/T/codex-clipboard-7a045321-0e22-4895-be92-dfb8b629a6a5.jpg`
- 视觉源（课程详情）：`/var/folders/dq/56xlfzsx05zc7vqhbl7lgv6c0000gn/T/codex-clipboard-c24b41fb-ae19-45c5-b342-3669af0e299a.jpg`
- 本轮补充视觉源（报名状态位置）：`/var/folders/dq/56xlfzsx05zc7vqhbl7lgv6c0000gn/T/codex-clipboard-92a89db9-b36b-4cd8-96fe-f7945578e823.png`
- 本轮补充视觉源（需移除的详情说明）：`/var/folders/dq/56xlfzsx05zc7vqhbl7lgv6c0000gn/T/codex-clipboard-5fe0107d-c6db-424a-becd-2cce971d026c.png`
- 实现截图（课程列表）：`/private/tmp/avatar-enrollment-list.png`
- 实现截图（课程详情）：`/private/tmp/avatar-enrollment-detail.png`
- 视觉源像素：列表 `1140 × 1456`；详情 `1140 × 2616`
- 实现截图像素：列表、详情均为 `780 × 1506`
- 实现 CSS 视口：约 `390 × 753`，微信开发者工具截图密度约 `@2x`
- 密度归一：参考详情按约 `@3x` 归一到 `380px` CSS 宽，与实现 `390px` 宽比较；列表源为内容裁切，只比较课程行内容区域，不比较裁切高度。
- 状态：真实生产课程数据；列表为未报名课程，详情为含 2 个试听课节且已有学习进度的锁定课程。
- 已知状态差异：真实数据只覆盖了“未报名”视觉状态；“已报名”状态使用同一徽标布局和黄色状态令牌，已通过模板分支与样式静态检查。

## 全视图比较证据

- 课程列表已形成“左侧 3:4 封面＋右侧课程名／学到哪／讲数和百分比”的单行结构，整行可点击；参考图中的播放按钮和三点菜单均未出现。
- 课程详情使用原生导航栏承载唯一课程标题；内容顶部仅保留学习位置、总讲数、百分比和进度条，没有“继续播放”模块。
- 报名状态已从详情页迁移到一级课程卡右上角；详情页不再显示课程介绍、报名说明或“联系运营开通完整课程”按钮。
- 课节使用一个连续白色面板和行分隔线，顶部提供微信原生“只看未学完”开关；课节行没有播放图标和三点菜单。
- 米白、黄色和圆角卡片沿用本产品既有视觉系统，没有照搬参考图的红色导航或第三方品牌装饰。

## 聚焦区域比较证据

- 列表卡：封面为 `144 × 192rpx`、标题最多两行、学习位置单行省略、底部元信息靠下对齐；实际截图中的长标题、试听状态和兜底封面均未发生裁切或重叠。
- 详情进度区：实际截图显示“学到：第 2 讲”、`已学 50%` 和对应进度条；旧生产接口缺少新字段时也能由课节数据安全推导，不再出现空百分比。
- 课节区：完整课节序号在过滤前生成，开启过滤后不会重新编号；已学完、部分学习、未学习、锁定和试听均有文字状态，不只依赖颜色。

## 必查视觉面

- 字体与层级：使用微信系统中文字体；课程标题、学习位置、进度、课节标题和元信息层级清楚。与参考图相比字重略厚，属于产品既有视觉风格，无 P0/P1/P2 问题。
- 间距与布局：列表采用紧凑横向行，详情采用进度卡＋课节面板；点击区域均明显大于 `88rpx`。删除报名说明卡后，进度与课节面板连续、留白稳定。
- 颜色与令牌：米白背景、白色卡片、黄色进度条和橙色百分比符合现有品牌色；文字对比度足够，状态同时有文案。
- 图片质量：课程缺图时使用真实位图兜底封面，无 CSS 图形、Emoji、手写 SVG 或透明边缘；压缩后为 `360 × 480`、约 `216KB`，在实际卡片尺寸下清晰。
- 文案内容：列表显示“学到哪／共几讲／已学多少”和“已报名／未报名”，详情只显示学习位置、百分比和课节状态；未购买课程仍可识别试听课节，不泄露付费内容。
- 图标与控件：按用户要求移除了课程和课节的播放、三点图标；筛选使用微信原生 switch，默认关闭。
- 响应与可访问性：长标题限制两行、学习位置单行省略；整行有按钮语义和可读标签；状态不只依赖色彩。

## Findings

- [P3] 列表表面比参考图更有卡片感
  - 位置：`miniprogram/miniprogram/pages/courses/index.wxss`
  - 证据：参考图接近无边框平铺列表，实现保留了轻边框、圆角和极浅阴影。
  - 影响：不影响信息结构和操作，且与平台既有米白圆角风格一致。
  - 后续可选：如希望进一步贴近参考图，可把阴影去掉并将行间距减小约 `4rpx`。

## 比较历史

### 第 1 轮

- 发现：[P1] 当前生产后端仍返回旧课程摘要字段，详情进度区显示“已学 %”。
- 修复：增加共享 `normalizeCourseProgress`，新接口字段缺失时从课节完成状态推导百分比和当前学习课节；列表与详情共用同一兼容逻辑。
- 修复后证据：`/private/tmp/avatar-course-detail-final.jpg` 显示“学到：第 2 讲”和“已学 50%”，进度条为 50%。

### 第 2 轮

- 发现：锁定课程的报名说明出现在详情页，一级课程页无法直接判断是否报名。
- 修复：删除详情页说明与联系按钮；复用 `CourseSummary.has_access`，在一级课程卡右上角显示“已报名／未报名”。
- 修复后证据：`/private/tmp/avatar-enrollment-list.png` 显示右上角“未报名”，标题和试听标签均无重叠；`/private/tmp/avatar-enrollment-detail.png` 显示进度卡下方直接进入课节列表。

## 残余测试缺口

- 微信开发者工具自动化 SDK 能完成真实编译和截图，但其旧版 `Page.callMethod` 与当前开发者工具不兼容，无法自动点击 switch；筛选纯函数已通过编译后 Node 断言，WXML 绑定已检查，仍建议在下一次真机预览时手点一次开关确认动画和触感。

## Implementation Checklist

- [x] 列表改为横向课程行，移除播放和三点菜单。
- [x] 整个课程行点击进入详情。
- [x] 详情顶部显示学习位置、讲数、百分比和进度条。
- [x] 删除“继续播放”模块。
- [x] 增加“只看未学完”，仅排除 `completed=true`。
- [x] 返回播放器后重新加载进度并保留当前筛选状态。
- [x] 列表和详情只统计已发布课节。
- [x] 一级课程卡右上角显示报名状态，试听状态仍独立显示。
- [x] 详情页移除报名说明和联系运营按钮。
- [x] 真实微信预览编译通过，包体 `312.6KB`。

final result: passed
